[project]
name = "comfy-modal"
version = "0.1.0"
description = "ComfyUI on Modal: CPU GUI, on-demand GPU worker, persistent volumes"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.5",
    "aiohttp>=3.9",
    "questionary>=2.1.1",
    "rich>=15.0.0",
]

[tool.uv]
package = false
